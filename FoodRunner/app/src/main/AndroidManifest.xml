<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.alexander.foodrunner">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_icon_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:resizeableActivity="false"
        android:roundIcon="@mipmap/ic_icon_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodRunner"
        tools:targetApi="n">
        <activity android:name=".activity.OrderSuccessActivity"></activity>
        <activity android:name=".activity.ResetPasswordActivity" />
        <activity android:name=".activity.MyCartActivity" />
        <activity android:name=".activity.RestaurantDetailsActivity" />
        <activity android:name=".activity.RestaurantDetails" />
        <activity android:name=".activity.ResFoodMenu" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.DisplayForgotActivity" />
        <activity android:name=".activity.ForgotActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>